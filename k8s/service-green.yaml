apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-api
  labels:
    app: aceest-fitness-api
spec:
  type: NodePort
  selector:
    app: aceest-fitness-api
    slot: green
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30050
